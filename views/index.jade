extends layout

block content


  //Wrapper
  #wrapper
    //Problems
    #problemList
      div(class="table-responsive-sm")
        table(class="table table-bordered")
          thead
            th(scope="col") Problem
            th(scope="col") Solution
            th(scope="col" width="4%") Count
            th(scope="col" width="7%") Add Count
            th(scope="col" width="2%")
              center
                img(src="/images/pencil.svg")
            th(scope="col" width="2%")
              center
                img(src="/images/trashcan.svg")

          tbody
    // /Problems
  // /Wrapper
